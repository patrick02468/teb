#include <stdio.h>
#include <stdlib.h>

int main(void)

{
	/*int a, b, c, max;
	scanf("%d%d%d", &a, &b, &c);
	if (a > b && a > c) {
		max = a;
	}
	if (b > a && b > c) {
		max = b;
	}
	if (c > a && c > a) {
		max = c;
	}
	printf("max  %d\n", max);
	system("pause");
	return 0;*/
	int a, b, c;
	scanf("%d%d%d", &a, &b, &c);
	printf("%d%d%d\n",a, b, c);
}